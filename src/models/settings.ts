type Settings = {
  cachedLyrics: number;
  maxLines: number;
  singlePlayer: boolean;
  minutes: number;
  seconds: number;
  playerNameStore: string[];
  playerKeyBindings: string[][];
};

export default Settings;
